# AGENTS — `src/print/model/`

Pure print rules: packet shape, this-week order, filenames, QR/image data URLs, PDF concat. `includeAnswerKey` is staff-only for page quizzes. No React document tree here — that lives under `print/print/`.
