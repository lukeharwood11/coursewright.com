# AGENTS — `src/resources/resource-edit/`

Edit a resource: title, link URL, Lexical document body, file title rename. Header actions match material edit: **Save**, desktop **Save & close**, and **Cancel** (reads **Close** when unchanged).
