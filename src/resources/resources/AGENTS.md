# AGENTS — `src/resources/resources/`

Resources home and folder browse (same screen; folder id from the route). Folder pane, path bar, **+ New**, row menus, dropzone.

`ResourcesLayout` hosts the upload queue. Pass auth `Outlet` context through so `useAuthedUser` still works.
