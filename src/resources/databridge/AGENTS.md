# AGENTS — `src/resources/databridge/`

PostgREST + Storage for `org_resource_folders`, `org_resource_items`, `org_resource_blocks`, `org_resource_grants`. File blobs go through `materials/databridge/files` and `infrastructure/supabase/storage`. Soft-archive only.
