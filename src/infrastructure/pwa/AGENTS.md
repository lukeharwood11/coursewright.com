# AGENTS — `src/infrastructure/pwa/`

Register the production service worker (`public/sw.js`). The worker shows Activity push; product rules for who is notified stay in `notifications/` and Postgres. Keep this a thin install helper.
