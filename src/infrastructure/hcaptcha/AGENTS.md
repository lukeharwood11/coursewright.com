# AGENTS — `src/infrastructure/hcaptcha/`

Publishable **hCaptcha site key** from `VITE_HCAPTCHA_SITE_KEY`. The secret stays in Supabase Auth (not in git).

Auth UI loads the invisible widget from `auth/components/AuthHcaptcha.tsx`.
