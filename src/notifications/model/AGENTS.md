# AGENTS — `src/notifications/model/`

Pure notification rules: paths, unread sort, header-bell preview, preview text, headlines by kind (discussion, mention, announcement), and whether this device is an installed PWA that should see a push prompt. No React, no Supabase.
