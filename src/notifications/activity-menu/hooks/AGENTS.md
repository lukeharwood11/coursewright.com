# AGENTS — `src/notifications/activity-menu/hooks/`

Wire unread Activity preview + ack into the header bell. Reuse `notificationQueryKeys` so org chrome Realtime invalidation refreshes the dropdown.
