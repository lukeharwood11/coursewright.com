# AGENTS — `src/events/databridge/`

PostgREST for `events`, `event_blocks`, and `event_materials`. Soft-delete events and blocks. Replacing linked materials deletes join rows. Never hard-delete an event from the app.
