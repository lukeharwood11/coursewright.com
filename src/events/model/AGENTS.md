# AGENTS — `src/events/model/`

Pure event rules: paths, audience (one course, classes, or the organization; which events a family should see), schedule (dates, optional times, required location), and draft validation. No React, no Supabase.
