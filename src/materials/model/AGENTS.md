# AGENTS — `src/materials/model/`

Pure material rules: kinds, visibility, block body shapes, playback, paths, validation. Rich text is `{ markdown }` until FEATURES locks a store. Video blocks are URL embeds.
