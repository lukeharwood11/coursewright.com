# AGENTS — `src/materials/material/`

View and edit a material (page / link / file). Page content uses the Lexical editor (headings, lists, quotes, tables, links, video URLs, in-page files, **quizzes** with correct answers). Save lives in the header and writes a version only when something changed. Print and Share stay visible. Parents see quiz questions only; staff see the answer key.
