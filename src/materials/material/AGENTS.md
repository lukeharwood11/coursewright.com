# AGENTS — `src/materials/material/`

View and edit a material (page / link / file). Page content uses the Lexical editor; Save lives in the header and writes a version only when something changed. Print and Share stay visible.
