# AGENTS — `src/roster/family/`

One **family** household in the org parent directory.

Same shape as a Class: group students (and parents) for browsing. **Do not** treat membership as course access. Linking a parent still writes `parent_student_links` (create or reuse). Removing a member does not drop those links or enrollments.
