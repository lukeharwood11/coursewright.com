# AGENTS — `src/calendar/components/`

Shared week/month calendar UI used by the Calendar page and parent This week. This week passes `layout="cards"` so empty days drop out and remaining **class-day** cards wrap (plan text + that class’s materials in the same card).
