# Shared public client defaults for `npm run dev` (testing branch).
# Safe to commit — these are browser-exposed publishable keys, not secrets.
# Override locally with `.env.local` (gitignored). Production builds use CI/hosting env.

# Supabase — persistent testing branch (Terraform output supabase_project_ref)
VITE_SUPABASE_URL=https://yplmaauelutcosqqvnya.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_OiFg7JIuGMlgIBHCXQo7hA_WScZEtov

# Public hostname for legal copy (privacy policy, etc.)
VITE_PUBLIC_HOST=beta.coursewright.com

# PostHog — disabled for local/testing (no key → SPA no-ops).
# Production builds get VITE_POSTHOG_* from the GitHub `production` Environment.
